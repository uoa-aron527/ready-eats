import React, { useState } from "react";
import {SearchForm} from './SearchForm'

const Search = () => {
  return (
    <SearchForm>
      <div>
        <input type="text" />
      </div>
    </SearchForm>
  );
};

export default Search;
